{% extends 'base.html.twig' %}
{% block body %}
    <h3> User {{ user.name }}</h3>
    <div class="row">
         <div class="col-4">
             {{ form(registrationPage) }}
         </div>
    </div>
{% endblock %}